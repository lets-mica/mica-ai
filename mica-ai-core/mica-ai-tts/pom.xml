<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>mica-ai-tts</artifactId>
    <name>${project.artifactId}</name>
    <url>https://www.dreamlu.net</url>
    <description>mica-ai-tts: 基于 Kokoro-82M 的纯 ONNX Runtime 语音合成引擎，支持中英双语</description>

    <parent>
        <groupId>net.dreamlu</groupId>
        <artifactId>mica-ai-core</artifactId>
        <version>${revision}</version>
    </parent>

    <dependencies>
        <!-- mica-ai common -->
        <dependency>
            <groupId>net.dreamlu</groupId>
            <artifactId>mica-ai-common</artifactId>
        </dependency>
        <!-- ONNX Runtime Java API -->
        <dependency>
            <groupId>com.microsoft.onnxruntime</groupId>
            <artifactId>onnxruntime</artifactId>
        </dependency>
        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
            G2P 库依赖（houbb/pinyin），默认 G2P 使用，强制依赖。
        -->
        <dependency>
            <groupId>com.github.houbb</groupId>
            <artifactId>pinyin</artifactId>
            <version>0.4.0</version>
        </dependency>
    </dependencies>

</project>
